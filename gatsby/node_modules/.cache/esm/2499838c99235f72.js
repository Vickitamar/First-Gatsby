let dotenv;_992‍.x([["default",()=>_992‍.o]]);_992‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

dotenv.config({ path: '.env' });

_992‍.d({
    siteMetadata: {
        title: `Slicks slices`,
        siteUrl: `https://gatsby.pizza`,
        description: `The best pizza place in Hamilton`,
    },
    plugins: [
        'gatsby-plugin-styled-components',
        {
            resolve: 'gatsby-source-sanity',
            options: {
                projectId: 'npfx97lq',
                dataset: 'production',
                watchMode: true,
                token: process.env.SANITY_TOKEN,
            }
        }
    ]
});